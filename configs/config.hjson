{
    # hjson formatted config file
    "db" : {
        # mongodb (or psql in future)
        # specific config data depends on type
        "type" : "mongodb"
        "mongoConfig" :{
            # full endpoint: address + port
            "URL" : "mongodb://127.0.0.1:27017"
            "ProjectsDBName" : "Projects"
            "SchemasDBName" : "Schemas"
        }
    }
}